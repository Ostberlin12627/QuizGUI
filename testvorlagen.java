public class testvorlagen {


    //zum erstellen eines Dropdownmenus (ewentuell für für das Optionsmenu)
   /* dropdown.addActionListener(new ActionListener() {
        @Override
        public void actionPerformed(ActionEvent e) {
            String selectedOption = (String) dropdown.getSelectedItem();
            switch (selectedOption) {
                case "Option 1":
                    // Führe Aktionen für Option 1 aus
                    break;
                case "Option 2":
                    // Führe Aktionen für Option 2 aus
                    break;
                case "Option 3":
                    // Führe Aktionen für Option 3 aus
                    break;
                // Weitere cases für weitere Optionen...
                default:
                    // Führe Aktionen aus, wenn keine Option ausgewählt wurde
                    break;
            }
        }
    });
*/


}
